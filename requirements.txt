# Core (server + relay responder): fastapi + uvicorn + httpx
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
# Tests: anthropic / openai / boto3 (SDK compatibility checks). boto3 is what makes
# puppetllm/tests/test_boto3_interop.py run — without it the SDK-level tests silently skip, and
# those are the only ones that prove botocore can parse the Bedrock event stream and the
# S3 XML (every other test uses puppetllm's own codec on both sides).
anthropic>=0.40
openai>=1.40
# 1.43 is the first bedrock-runtime model with everything the SDK tests exercise
# (serviceTier / performanceConfigLatency on InvokeModel, outputConfig, requestMetadata);
# an older floor imports fine and then fails those tests client-side.
boto3>=1.43
