# Core (server startup): fastapi + uvicorn
fastapi>=0.115
uvicorn[standard]>=0.30
# Tests: httpx (ASGITransport) + anthropic / openai (SDK compatibility checks)
httpx>=0.27
anthropic>=0.40
openai>=1.40
